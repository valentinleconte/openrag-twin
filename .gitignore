# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# RSA keys for JWT signing
*.pem

# openrag-twin: local backend API key used by scripts/twin/up.sh's smoke
# test and by manual demo curl calls. Not a repo secret in the .env sense,
# but still a live credential for this local instance — never commit it.
.orag_apikey

.idea/

1001*.pdf
*.json
!**/package.json
!**/package-lock.json
!**/tsconfig.json
!flows/*.json
!src/tui/_assets/flows/*.json
!src/tui/_assets/flows/components/*.json
!frontend/*.json
.DS_Store
logs

/config/

.docling.pid

# OpenSearch data directory
opensearch-data/
# Langflow data directory
langflow-data/

node_modules

# AI Tools
/.claude
/CLAUDE.md
/.gemini
/GEMINI.md
/.bob
/AGENTS.md
documents/warmup_ocr.pdf
documents/openrag-documentation.pdf
documents/ibm_anthropic.pdf
documents/docling.pdf
/opensearch-data-new-lf
/opensearch-data2
*.db
