{{- if and .Values.backend.enabled .Values.backend.persistence.data.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "openrag.fullname" . }}-data
  namespace: {{ include "openrag.namespace" . }}
  labels:
    {{- include "openrag.backend.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.backend.persistence.data.accessMode }}
  {{- if .Values.backend.persistence.data.storageClass }}
  storageClassName: {{ .Values.backend.persistence.data.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.backend.persistence.data.size }}
{{- end }}
